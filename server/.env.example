# Pick one provider (or set both; provider can be chosen per request)
XAI_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Defaults
OMNI_PROVIDER=xai
OMNI_MODEL=grok-4
PORT=8787
WORKSPACE_ROOT=./workspace
